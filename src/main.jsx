import React from "react";
import ReactDOM from "react-dom/client";
import AirbrickDashboard from "./AirbrickDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AirbrickDashboard />
  </React.StrictMode>
);
